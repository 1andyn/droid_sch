<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uhmanoa.droid_sch" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity android:name=".Main_menu" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Create_Schedules"
            android:label="@string/title_activity_create__schedules" >
        </activity>
        <activity
            android:name=".Available_Schedules"
            android:label="@string/title_activity_available__schedules" >
        </activity>
    </application>

</manifest>
